package CollectorExercises13;

import java.util.*;

/**
 * Created by barto on 27/06/2017.
 */
public class CountIntegersInCue {
    public static void main(String[] args) {
/*
        List<Integer> intArray = new ArrayList<>();
        List<Integer> uniqueArray = new ArrayList<>();
        HashMap<Integer, Integer> intMap = new HashMap<>();
        Scanner s = new Scanner(System.in);


        System.out.println("Geef een eerste integer");
        Integer number = s.nextInt();
        while (number!=0) {
            System.out.println("Geef nog een integer of druk '0' om te eindigen.");
            number=s.nextInt();
            intArray.add(number);
            intMap.entrySet();
        }
        uniqueArray.addAll(intArray);
        uniqueArray.retainAll(intMap);
        for(int i=0;i<uniqueArray.size(); i++){
            System.out.println(" " + uniqueArray.get(i));
        }

        int[] countInt = new int[intMap.size()];
        for (int i:countInt){i=0;};
        for (int i=0; i<intMap.size();i++){
            while (intArray.contains(uniqueArray.get(i))){
                countInt[i]++;
            }
            }

        System.out.println(countInt);
*/

    }
}
